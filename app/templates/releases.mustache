<!DOCTYPE html>
<html>

<head>
    <title>My App Releases</title>
</head>

<body>
<div>
  {{#latest_release}}
    <h2>Current release</h2>
    <h3>{{name}} - {{version}}</h3>
    <p>Published {{pub_date}}</p>
    <p>{{notes}}</p>
    <a href="{{url}}">Download</a>
  {{/latest_release}}
  {{^latest_release}}
    <p>No release yet.</p>
  {{/latest_release}}
</div>

<h2>Create new release</h2>
<div>
  <form action="/admin/releases" method="POST">
    <input type="text" name="name" placeholder="Name"/>
    <input type="text" name="version" placeholder="Number"/>
    <input type="textarea" name="notes" placeholder="Notes"/>
    <input type="text" name="url" placeholder="URL"/>
    <input type="submit"/>
  </form>
</div>
</body>

</html>
